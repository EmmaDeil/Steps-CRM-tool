// src/firebase.js
// Firebase was removed from this project. Kept for compatibility
// with any lingering imports during the transition. Use
// `src/noFirebase.js` for the minimal stubbed behavior instead.

export const placeholder = null;

export default {};